<div class="container m-5 p-5">
    <div class="row d-flex justify-content-center my-2 text-center">
        <form action="http://localhost:8080/restablecercontrasena" method="post" class="border col-8 form-color">
            <div class="row d-flex justify-content-center my-2 text-center">
                <h5>Ingresa tu correo y te enviaremos un link para restablecer tu contraseña</h5>
            </div>
            {{#if msg}}
            <div class="row my-5 d-flex justify-content-center">
                <div class="col-md-8">
                    <p class="fw-bolder text-success">{{msg}}</p>
                </div>
            </div>
            {{/if}}
            <div class="row d-flex justify-content-center my-2">

                <div class="form-floating mb-3 col-md-8">
                    <input type="email" class="form-control" id="userEmail" name="userEmail"
                        placeholder="name@example.com">
                    <label for="floatingInput">Email</label>
                </div>
            </div>
            <div class="row my-4 d-flex justify-content-center">
                <div class="col-md-8">
                    <button class="btn btn-primary" type="submit">Enviar</button>

                </div>
            </div>
        </form>
    </div>
</div>